import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as SectionContainerStories from "./section-container.stories";

<Meta of={SectionContainerStories} />

# SectionContainer

セクション単位のコンテンツを枠付きでグルーピングするコンポーネントです。

## Examples

### Default
<Canvas of={SectionContainerStories.Default} />
<Controls of={SectionContainerStories.Default} />

