import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as PageTopButtonStories from "./page-top-button.stories";

<Meta of={PageTopButtonStories} />

# PageTopButton

ページ最下部付近からトップへ戻るためのフローティングボタンです。クリックでスムーズスクロールします。

## Examples

### Default
<Canvas of={PageTopButtonStories.Default} />
<Controls of={PageTopButtonStories.Default} />
