import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as TabsStories from "./tabs.stories";

<Meta of={TabsStories} />

# Tabs

関連するコンテンツをタブ単位で切り替えるコンポーネントです。

## Examples

### Default
<Canvas of={TabsStories.Default} />
<Controls of={TabsStories.Default} />
