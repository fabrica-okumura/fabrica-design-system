# Button

基本的なアクション実行用コンポーネントです。  
このページは「人が読むための概要・使い方」を記載し、バリエーションや状態確認の詳細は Storybook を参照します。

**Storybook（UI/Button）:** [http://localhost:6006/?path=/docs/ui-button--docs](http://localhost:6006/?path=/docs/ui-button--docs)

## Usage

```tsx
import { Button } from "@fabrica_communications/design-system";

export default function Example() {
  return <Button>保存</Button>;
}
```
